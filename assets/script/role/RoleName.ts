import { _decorator, Component, RenderTexture, Label, Billboard, Camera } from 'cc';
const { ccclass, property } = _decorator;

@ccclass('RoleName')
export class RoleName extends Component {
    
    start(){
        
    }

    public setText(text: string): void {
    
    }
}

